<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Contact</title>
</head>
<body>
    <%- include('header'); %>
    <h1>Please contact <%= user.name %></h1>
    <h2>via <%= user.email %></h2>
    <h3>Skills</h3>
    <ul>
        <% user.skills.forEach((item)=>{ %>
            <li><%= item %></li>
        <% }) %>
    </ul>
</body>
</html>